# LoCoMo-MC10 Benchmark Dependencies
#
# PINNED 2026-08-28. These were `>=` floors, so the benchmark's dependency surface was whatever
# PyPI resolved on the day somebody ran it — not reproducible, and not auditable. It also made
# every CVE reported against this file a statement about a version the repository never declared:
# osv-scanner resolves unpinned floors itself and reported `aiohttp@3.9.5`, which this file does
# not name, as 35 of shodh-memory's 48 dependency findings including its only critical.
#
# Pin, and the scanner reports what this file actually says.

datasets==5.0.1
tqdm==4.70.0

# shodh-memory itself. This name IS this project's on PyPI (varun29ankuS/shodh-memory, latest
# 0.2.0) — verified against the PyPI JSON API on 2026-08-28. Recorded because the veld twin's
# equivalent line is NOT: `veld` on PyPI is an unrelated project, and that divergence is a property
# of the rename rather than of either codebase.
shodh-memory==0.2.0

# ── LLM providers (install the one you need) ───────────────────────────────────────────────────
openai==3.5.0
# anthropic==1.2.0    # for Anthropic Claude (optional)
# ollama==0.6.2       # for Ollama local models (optional)
# requests==2.34.2    # for Baseten native API (optional)
